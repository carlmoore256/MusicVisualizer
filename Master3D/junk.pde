  //r = ((cos(colorShift) + 1) / 2) * 255; 
  //g = ((cos(colorShift - colorSpread) + 1) / 2) * 255; //color spread makes the colors always related
  //b = ((cos(colorShift + colorSpread) + 1) / 2) * 255; 
  //gradientStart = color(r,g,b);
  
    //    if(pitches.get(i) == pitch){ //if there is already a note value with this at it, reset it
    //    pitches.set(i, pitch);
    //    sizes.set(i, pitch);
    //    velocities.set(i, velocity);
    //  } else {
    //    pitches.append(pitch);
    //    velocities.append(velocity);
    //    sizes.append(pitch);
    //  }
    //}
    //numVoices = pitches.size(); //set voices to list size
